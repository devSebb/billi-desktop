<div class="flex items-center justify-between">
  <div class="flex items-center gap-3">
    <div class="w-8 h-8 rounded-lg bg-gray-50 flex items-center justify-center text-lg"><%= icon %></div>
    <span class="text-gray-600 font-medium"><%= label %></span>
  </div>
  <span class="font-medium text-brand-charcoal"><%= number_to_currency(amount, precision: 0) %></span>
</div>

