development:
  adapter: async

test:
  adapter: test

# Production: set REDIS_URL on Render (add a Redis instance and link it). Required for Action Cable and Sidekiq.
production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: billi_production
